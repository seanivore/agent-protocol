# Project Name — Goals & North Star (the review yardstick)

**What this is**: the durable capture of *why this project (or this initiative within it) exists and what "done" means as a whole product* — the thing every gap review measures the build against. The build docs (`assets/docs/archive/…`) say *how*; this says *what for*. When a reviewer asks "is this done," they measure against THIS — not against the build's own internal consistency. Sourced from [the concept document / high-level requirements / descriptive UX-UI writing that started this].

*(This doc is never written once and closed. A project's goals accumulate at its start; each new initiative inside the project adds its own north star from its own concept document — same mechanism, same file. See `~/.agents/protocol/GAP_REVIEW.md` for how reviews use it.)*

---

## Enduring project goals (the why — these outlast any single build)

- [North star 1 — what this project is fundamentally for, in the user's own words where possible]
- [North star 2]
- [Constraints that shape everything: budget, platform commitments, non-negotiables]

## This build's (or initiative's) North Star

[What "done" looks like for the current slice of work — one paragraph.]

## The whole-product intent (the part reviews keep missing)

**This is not a bag of features; it's a system whose pieces feed each other.** The per-feature work being built is table-stakes. The *goal* is the value loops the vision implies — and "done" means these are **wired**, not just present side-by-side:

- [Value loop 1 — e.g. "output of feature A becomes the input to feature B"]
- [Value loop 2]

## The standing mandate: requirements are NOT capped

A gap review's job includes **finding the capabilities and connections the plan doesn't yet have** — not only verifying that what's written works.

## How reviews use this doc

Angle A (and any product-holism pass) measures the build against THIS: *does it deliver these goals, and are the pieces connected into the system described above?* **Missing functionality and missing valuable interconnections are first-class gaps** — especially early, before code exists, when integration is cheapest to add.

---
*Living doc. Update the North Star at each new build or initiative; keep the enduring goals + whole-product intent current as the vision sharpens. Hand this to every reviewer.*
